/# a playground for testing kiwi #/

println "hello world!".chars().map(with (c) do
  return 0 when c == "o"
  return 1 when c == "l"
  return 3 when c == "e"
  return c
end).join()

println __rlist__().packages.sort()