@hash = {
  "key1": true, 
  "key2": 1, 
  "key3": ["a", "b", "c"]
}

@otherHash = {}

for @key in @hash.keys() do
  @otherHash[@key] = @hash[@key]
end

println @otherHash

@keys = @otherHash.keys()
println @keys

println ""