println "=============="
println "= lists.kiwi ="
println "=============="

@a = [1, 3, "hello, world!", [5, "asdf", true], [2, 4]]
@b = [1, 2, 3, 4]
@c = @a[2]
@d = @a[0:1000000] # Probably needs to throw IndexError (shrug)

println "a = ${@a}"
println "b = ${@b}"
println "c = ${@c}"
println "d = ${@d}"

@b = @b[::-1]
println "reversed b = ${@b}"

@a = @a[::-1]
println "reversed a = ${@a}"

# Iterate a list.
println "iterating a:"
@i = 0
while @i < 5 do
  @item = @a[@i]
  println "${@i}: ${@item}"
  @i += 1
end

println ""