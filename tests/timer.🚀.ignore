# rename this file to .🚀 or .astral to run it.

import "@astral/time"

i = 0
while i < 10 do
  ts = time::timestamp()
  println "Current timestamp: ${ts}"
  time::delay(1000)
  i += 1
end

println ""