#!/usr/bin/astral
/# This 🚀 script is for experimentation. #/

"hello".chars().each(with (c, i) do
  println("${i} = ${c}")
end)

[1..3].each(with (i) do println(i) end)

[10, 20, 30].each(with (i) do println(i) end)