##
By listing the first six prime numbers: 2, 3, 5, 7, 11, and 13, we can see that the 6th prime is 13.

What is the 10,001 prime number?
##


@count = 0
@num = 1
@limit = 10001

is_prime = lambda (@n) do
  return @num % @n == 0
end

while @count < @limit do
  @num += 1
  
  if @num <= 1
    next
  end

  if @num <= 3
    @count += 1
    next
  end

  @set = [2 .. __sqrt__(@num).to_i()]
  @prime = @set.none(lambda (@n) do
    return @num % @n == 0
  end)
  
  if @prime
    @count += 1
  end
end

println @num